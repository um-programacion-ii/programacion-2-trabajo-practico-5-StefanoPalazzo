# src/main/resources/application.yml
spring:
  datasource:
    url: jdbc:h2:mem:tp5db
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
